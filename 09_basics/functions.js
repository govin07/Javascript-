let govind = 20;
let aditya = 19;
if(govind > aditya){
    whoIsBig(govind)
}

function whoIsBig(user){
    console.log(`${user}, your are big mannn`)
    for (let i = 0; i <= 20; i++) {
        console.log(`${user}, your age is ${i}`)
        
    }};